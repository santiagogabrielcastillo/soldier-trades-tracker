<div>
  <%= render PageHeaderComponent.new(title: t("stocks.index.title")) do |c| %>
    <% c.with_actions do %>
      <% if @all_portfolios.size > 1 %>
        <div class="flex items-center gap-2">
          <%= label_tag :portfolio_id, t("stocks.index.portfolio_label"), class: "text-sm text-slate-600" %>
          <%= select_tag :portfolio_id,
                options_for_select(@all_portfolios.map { |p| [p.name, p.id] }, @stock_portfolio.id),
                onchange: "window.location.href='#{stocks_path}?portfolio_id=' + this.value",
                class: "rounded-md border border-slate-300 px-3 py-1.5 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500" %>
        </div>
      <% end %>
      <%= render ButtonComponent.new(label: t("stocks.index.manage_portfolios"), href: stock_portfolios_path) %>
    <% end %>
  <% end %>

  <% if @stock_portfolio.argentina? %>
    <div class="mb-4 flex flex-wrap items-center gap-3">
      <% if @mep_rate %>
        <span class="inline-flex items-center rounded-full bg-blue-50 px-3 py-1 text-xs font-medium text-blue-700 ring-1 ring-inset ring-blue-600/20">
          <%= t("stocks.index.mep_rate", rate: format_ars(@mep_rate)) %>
        </span>
      <% else %>
        <span class="inline-flex items-center rounded-full bg-amber-50 px-3 py-1 text-xs font-medium text-amber-700 ring-1 ring-inset ring-amber-600/20">
          <%= t("stocks.index.mep_unavailable") %>
        </span>
      <% end %>
      <%= link_to t("stocks.index.manage_cedear"), cedear_instruments_path, class: "text-sm text-slate-500 hover:text-slate-800" %>
    </div>
  <% end %>

  <%= render TabNavComponent.new(tabs: [
    { label: t("stocks.index.tabs.portfolio"),     url: stocks_path(stocks_index_filter_params.merge("view" => "portfolio",   "portfolio_id" => @stock_portfolio.id)), active: @view == "portfolio" },
    { label: t("stocks.index.tabs.transactions"),  url: stocks_path(stocks_index_filter_params.merge("view" => "transactions", "portfolio_id" => @stock_portfolio.id)), active: @view == "transactions" },
    { label: t("stocks.index.tabs.performance"),   url: stocks_path(stocks_index_filter_params.merge("view" => "performance",  "portfolio_id" => @stock_portfolio.id)), active: @view == "performance" },
    { label: t("stocks.index.tabs.valuations"),    url: stocks_path(stocks_index_filter_params.merge("view" => "valuations",   "portfolio_id" => @stock_portfolio.id)), active: @view == "valuations" },
    { label: t("stocks.index.tabs.watchlist"),     url: stocks_path(view: "watchlist"), active: @view == "watchlist" }
  ]) %>

  <% if @view == "portfolio" %>
  <div class="mb-6 flex flex-wrap items-center gap-3">
    <%= render ModalComponent.new(title: t("stocks.index.new_trade"), trigger_label: t("stocks.index.new_trade"), open_on_connect: @open_new_trade_modal.present?) do %>
      <%= form_with url: stocks_trades_path, method: :post, local: true, data: { turbo: false, controller: @stock_portfolio.argentina? ? "cedear-ratio" : nil, "cedear-ratio-lookup-url-value": lookup_cedear_instruments_path } do |f| %>
        <%= hidden_field_tag :portfolio_id, @stock_portfolio.id %>
        <div class="space-y-4">
          <div>
            <%= f.label :side, t("stocks.index.form.side"), class: "block text-sm font-medium text-slate-700" %>
            <%= f.select :side, options_for_select([[t("stocks.index.form.buy"), "buy"], [t("stocks.index.form.sell"), "sell"]], @stock_trade&.side), { include_blank: t("stocks.index.form.select_side") }, class: "w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500" %>
            <% if @stock_trade&.errors&.key?(:side) %>
              <p class="mt-1 text-sm text-red-600"><%= @stock_trade.errors[:side].first %></p>
            <% end %>
          </div>
          <div>
            <%= f.label :ticker, @stock_portfolio.argentina? ? t("stocks.index.form.ticker_bcba") : t("stocks.index.form.ticker"), class: "block text-sm font-medium text-slate-700" %>
            <%= f.text_field :ticker, value: @stock_trade&.ticker, placeholder: "e.g. AAPL", autocomplete: "off",
                  data: @stock_portfolio.argentina? ? { "cedear-ratio-target": "ticker", action: "blur->cedear-ratio#tickerChanged" } : {},
                  class: "w-full rounded-md border border-slate-300 px-3 py-2 text-sm uppercase focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500" %>
            <% if @stock_trade&.errors&.key?(:ticker) %>
              <p class="mt-1 text-sm text-red-600"><%= @stock_trade.errors[:ticker].first %></p>
            <% end %>
          </div>
          <div>
            <%= f.label :shares, @stock_portfolio.argentina? ? t("stocks.index.form.quantity") : t("stocks.index.form.shares"), class: "block text-sm font-medium text-slate-700" %>
            <%= f.number_field :shares, value: @stock_trade&.shares, step: :any, min: 0, placeholder: "0", class: "w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500" %>
            <% if @stock_trade&.errors&.key?(:shares) %>
              <p class="mt-1 text-sm text-red-600"><%= @stock_trade.errors[:shares].first %></p>
            <% end %>
          </div>
          <div>
            <%= f.label :price_usd, @stock_portfolio.argentina? ? t("stocks.index.form.price_ars") : t("stocks.index.form.price_usd"), class: "block text-sm font-medium text-slate-700" %>
            <%= f.number_field :price_usd, value: @stock_trade&.price_usd, step: :any, min: 0, placeholder: "0.00", class: "w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500" %>
            <% if @stock_trade&.errors&.key?(:price_usd) %>
              <p class="mt-1 text-sm text-red-600"><%= @stock_trade.errors[:price_usd].first %></p>
            <% end %>
          </div>
          <% if @stock_portfolio.argentina? %>
          <div>
            <label class="block text-sm font-medium text-slate-700"><%= t("stocks.index.form.cedear_ratio") %> <span class="font-normal text-slate-500"><%= t("stocks.index.form.cedear_ratio_hint") %></span></label>
            <input type="number" step="any" min="0" placeholder="e.g. 10"
                   data-cedear-ratio-target="ratio"
                   class="w-full rounded-md border border-slate-300 bg-slate-50 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500"
                   disabled>
            <p class="mt-1 text-xs text-slate-500"><%= t("stocks.index.form.cedear_ratio_auto") %></p>
          </div>
          <% end %>
          <div>
            <%= f.label :executed_at, t("stocks.index.form.executed_at"), class: "block text-sm font-medium text-slate-700" %>
            <%= f.datetime_local_field :executed_at, value: @stock_trade&.executed_at&.strftime("%Y-%m-%dT%H:%M"), class: "w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500" %>
            <p class="mt-1 text-xs text-slate-500"><%= t("spot.index.form.timezone_hint", zone: Time.zone.name) %></p>
            <% if @stock_trade&.errors&.key?(:executed_at) %>
              <p class="mt-1 text-sm text-red-600"><%= @stock_trade.errors[:executed_at].first %></p>
            <% end %>
          </div>
        </div>
        <div class="mt-6 flex gap-2">
          <%= f.submit t("stocks.index.form.add_trade"), class: "rounded-md bg-slate-800 px-4 py-2 text-sm font-medium text-white hover:bg-slate-700 focus:outline-none focus:ring-2 focus:ring-slate-500" %>
          <button type="button" data-action="dialog#close" class="rounded-md border border-slate-300 bg-white px-4 py-2 text-sm text-slate-700 hover:bg-slate-50"><%= t("common.cancel") %></button>
        </div>
      <% end %>
    <% end %>
  </div>

  <% if @positions.any? %>
  <div>
    <% if @stock_portfolio.argentina? %>
      <%# ── Argentina stat cards ── %>
      <%
        total_positions_ars = @stocks_value - @cash_balance
        total_positions_usd = @mep_rate&.positive? ? (total_positions_ars / @mep_rate) : nil
        pnl_usd             = @mep_rate&.positive? ? (@total_unrealized_pnl / @mep_rate) : nil
        cash_usd_stat       = @mep_rate&.positive? ? (@cash_balance / @mep_rate) : nil
      %>
      <%= render SummaryStatRowComponent.new(mb: 6) do %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.stocks_ars"), value: format_ars(total_positions_ars)) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.stocks_usd"), value: total_positions_usd ? format_money(total_positions_usd) : nil) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.unrealized_pnl_ars"), value: format_ars(@total_unrealized_pnl), signed: true, color_value: @total_unrealized_pnl) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.unrealized_pnl_usd"), value: pnl_usd ? format_money(pnl_usd) : nil, signed: true, color_value: pnl_usd) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.cash_ars"), value: format_ars(@cash_balance)) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.cash_usd"), value: cash_usd_stat ? format_money(cash_usd_stat) : nil) %>
      <% end %>
    <% else %>
      <%# ── US stat cards ── %>
      <%= render SummaryStatRowComponent.new(mb: 6) do %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.stocks_usd"), value: format_money(@stocks_value - @cash_balance)) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.unrealized_pnl"), value: format_money(@total_unrealized_pnl), signed: true, color_value: @total_unrealized_pnl) %>
        <%= render StatCardComponent.new(label: t("stocks.index.stats.cash_usd"), value: format_money(@cash_balance)) %>
      <% end %>
    <% end %>


    <% if @stock_portfolio.argentina? %>
      <%# ── Argentina CEDEAR portfolio table ── %>
      <% total_ars = @stocks_value %>
      <%= render DataTableComponent.new(columns: [
        { label: t("stocks.index.columns.ticker"),        classes: "text-left" },
        { label: t("stocks.index.columns.ratio"),         classes: "text-right" },
        { label: t("stocks.index.columns.quantity"),      classes: "text-right" },
        { label: t("stocks.index.columns.avg_buy_price"), classes: "text-right" },
        { label: t("stocks.index.columns.price_ars"),     classes: "text-right" },
        { label: t("stocks.index.columns.price_usd"),     classes: "text-right" },
        { label: t("stocks.index.columns.total_ars"),     classes: "text-right" },
        { label: t("stocks.index.columns.total_usd"),     classes: "text-right" },
        { label: t("stocks.index.columns.pct_total"),     classes: "text-right" },
        { label: t("stocks.index.columns.pct_pnl"),       classes: "text-right" }
      ]) do |table| %>
        <% @positions.each do |pos| %>
          <% current_price_ars = @current_prices[pos.ticker] %>
          <% current_price_usd = (@mep_rate && current_price_ars) ? (current_price_ars / @mep_rate) : nil %>
          <% total_pos_ars = current_price_ars ? current_price_ars.to_d * pos.shares : nil %>
          <% total_pos_usd = (@mep_rate && total_pos_ars) ? (total_pos_ars / @mep_rate) : nil %>
          <% pct_total = (total_pos_ars && total_ars&.positive?) ? (total_pos_ars / total_ars * 100) : nil %>
          <% pct_pnl = (current_price_ars && pos.breakeven&.positive?) ? ((current_price_ars.to_d - pos.breakeven.to_d) / pos.breakeven.to_d * 100) : nil %>
          <% ratio = @cedear_instruments[pos.ticker]&.ratio %>
          <% table.with_row do %>
            <td class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-slate-900"><%= pos.ticker %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= ratio ? number_with_precision(ratio, precision: 4, strip_insignificant_zeros: true) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= number_with_precision(pos.shares, precision: 6, strip_insignificant_zeros: true) %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= pos.breakeven ? format_ars(pos.breakeven) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= current_price_ars ? format_ars(current_price_ars) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= current_price_usd ? format_money(current_price_usd) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= total_pos_ars ? format_ars(total_pos_ars) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= total_pos_usd ? format_money(total_pos_usd) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-500"><%= pct_total ? number_to_percentage(pct_total, precision: 1) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm <%= pct_pnl.nil? ? 'text-slate-500' : (pct_pnl >= 0 ? 'text-emerald-600' : 'text-red-600') %>"><%= pct_pnl ? number_to_percentage(pct_pnl, precision: 2) : "—" %></td>
          <% end %>
        <% end %>
        <% if @cash_balance.nonzero? %>
          <% cash_usd = (@mep_rate && @cash_balance) ? (@cash_balance / @mep_rate) : nil %>
          <% cash_pct_total = @stocks_value.positive? ? (@cash_balance / @stocks_value * 100) : nil %>
          <% table.with_row do %>
            <td class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-slate-900">CASH</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm <%= @cash_balance.negative? ? 'text-red-600' : 'text-slate-700' %>"><%= format_ars(@cash_balance) %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= cash_usd ? format_money(cash_usd) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-500"><%= cash_pct_total ? number_to_percentage(cash_pct_total, precision: 1) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <% end %>
        <% end %>
        <%# ── Totals row ── %>
        <% total_usd = @mep_rate&.positive? ? (@stocks_value / @mep_rate).round(2) : nil %>
        <% table.with_row(classes: "border-t-2 border-slate-200 bg-slate-50") do %>
          <td class="whitespace-nowrap px-6 py-4 text-sm font-bold text-slate-900">TOTAL</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm font-bold text-slate-900"><%= format_ars(@stocks_value) %></td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm font-bold text-slate-900"><%= total_usd ? format_money(total_usd) : "—" %></td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
        <% end %>
      <% end %>
    <% else %>
      <%# ── US stock portfolio table ── %>
      <%= render DataTableComponent.new(columns: [
        { label: t("stocks.index.columns.ticker"),         classes: "text-left" },
        { label: t("stocks.index.columns.avg_cost"),       classes: "text-right" },
        { label: t("stocks.index.columns.shares"),         classes: "text-right" },
        { label: t("stocks.index.columns.current_price"),  classes: "text-right" },
        { label: t("stocks.index.columns.market_value"),   classes: "text-right" },
        { label: t("stocks.index.columns.pct_total"),      classes: "text-right" },
        { label: t("stocks.index.columns.unrealized_pnl"), classes: "text-right" },
        { label: t("stocks.index.columns.unrealized_roi"), classes: "text-right" }
      ]) do |table| %>
        <% @positions.each do |pos| %>
          <% current_price = @current_prices[pos.ticker] %>
          <% unrealized = current_price && pos.breakeven ? (current_price.to_d - pos.breakeven.to_d) * pos.shares : nil %>
          <% market_value = current_price ? current_price.to_d * pos.shares : nil %>
          <% pct_total = (market_value && @stocks_value.positive?) ? (market_value / @stocks_value * 100) : nil %>
          <% unrealized_roi = unrealized && pos.net_usd_invested.to_d.positive? ? (unrealized / pos.net_usd_invested.to_d * 100) : nil %>
          <% table.with_row do %>
            <td class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-slate-900"><%= pos.ticker %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= pos.breakeven ? format_money(pos.breakeven) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= number_with_precision(pos.shares, precision: 6, strip_insignificant_zeros: true) %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= current_price ? format_money(current_price) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= market_value ? format_money(market_value) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-500"><%= pct_total ? number_to_percentage(pct_total, precision: 1) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm <%= unrealized.nil? ? 'text-slate-500' : (unrealized >= 0 ? 'text-emerald-600' : 'text-red-600') %>"><%= unrealized ? format_money(unrealized) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm <%= unrealized_roi.nil? ? 'text-slate-500' : (unrealized_roi >= 0 ? 'text-emerald-600' : 'text-red-600') %>"><%= unrealized_roi ? number_to_percentage(unrealized_roi, precision: 2) : "—" %></td>
          <% end %>
        <% end %>
        <% if @cash_balance.nonzero? %>
          <% cash_pct_total = @stocks_value.positive? ? (@cash_balance / @stocks_value * 100) : nil %>
          <% table.with_row do %>
            <td class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-slate-900">CASH</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm <%= @cash_balance.negative? ? 'text-red-600' : 'text-slate-700' %>"><%= format_money(@cash_balance) %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-500"><%= cash_pct_total ? number_to_percentage(cash_pct_total, precision: 1) : "—" %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <% end %>
        <% end %>
        <%# ── Totals row ── %>
        <% table.with_row(classes: "border-t-2 border-slate-200 bg-slate-50") do %>
          <td class="whitespace-nowrap px-6 py-4 text-sm font-bold text-slate-900">TOTAL</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm font-bold text-slate-900"><%= format_money(@stocks_value) %></td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
          <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-400">—</td>
        <% end %>
      <% end %>
      <p class="mt-3 text-sm text-slate-500"><%= t("stocks.index.finnhub_hint") %></p>
    <% end %>
    <% if @stocks_chart_data[:pie].any? || @stocks_chart_data[:bar].any? %>
      <%= content_tag :div,
            class: "mt-6 grid grid-cols-1 gap-6 lg:grid-cols-2",
            data: { controller: "stocks-charts", "stocks-charts-data-value": @stocks_chart_data.to_json } do %>
        <div class="rounded-lg border border-slate-200 bg-white p-4 shadow-sm">
          <p class="mb-3 text-sm font-medium text-slate-700"><%= t("stocks.index.charts.portfolio_allocation") %></p>
          <div class="h-52">
            <canvas data-stocks-charts-target="pieCanvas"></canvas>
            <p class="hidden text-sm text-slate-500" data-stocks-charts-target="pieEmpty"><%= t("stocks.index.charts.no_price_data") %></p>
          </div>
        </div>
        <div class="rounded-lg border border-slate-200 bg-white p-4 shadow-sm">
          <p class="mb-3 text-sm font-medium text-slate-700"><%= t("stocks.index.charts.unrealized_pnl_by_ticker") %></p>
          <div class="h-52">
            <canvas data-stocks-charts-target="barCanvas"></canvas>
            <p class="hidden text-sm text-slate-500" data-stocks-charts-target="barEmpty"><%= t("stocks.index.charts.no_price_data") %></p>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
  <% else %>
    <%= render EmptyStateComponent.new(message: t("stocks.index.empty_positions")) %>
  <% end %>
  <% end %>

  <% if @view == "transactions" %>
    <%= form_with url: stocks_path, method: :get, local: true, class: "mb-4 flex flex-wrap items-end gap-4", data: { controller: "auto-submit-form", turbo: false } do |f| %>
      <%= f.hidden_field :view, value: "transactions" %>
      <%= hidden_field_tag :portfolio_id, @stock_portfolio.id %>
      <div class="flex flex-col gap-1">
        <%= f.label :from_date, t("common.from"), class: "text-sm font-medium text-slate-700" %>
        <%= f.date_field :from_date, value: @from_date, class: "rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500 w-auto min-w-[10rem]", data: { auto_submit_form_target: "trigger" } %>
      </div>
      <div class="flex flex-col gap-1">
        <%= f.label :to_date, t("common.to"), class: "text-sm font-medium text-slate-700" %>
        <%= f.date_field :to_date, value: @to_date, class: "rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500 w-auto min-w-[10rem]", data: { auto_submit_form_target: "trigger" } %>
      </div>
      <div class="flex flex-col gap-1">
        <%= f.label :ticker, t("stocks.index.columns.ticker"), class: "text-sm font-medium text-slate-700" %>
        <%= f.select :ticker, options_for_select([[t("common.all"), ""]] + (@tickers_for_filter || []).map { |tk| [tk, tk] }, @filter_ticker), {}, { class: "rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500 w-auto min-w-[8rem]", data: { auto_submit_form_target: "trigger" } } %>
      </div>
      <div class="flex flex-col gap-1">
        <%= f.label :side, t("stocks.index.form.side"), class: "text-sm font-medium text-slate-700" %>
        <%= f.select :side, options_for_select([[t("common.all"), ""], [t("stocks.index.form.buy"), "buy"], [t("stocks.index.form.sell"), "sell"]], @filter_side), {}, { class: "rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500 w-auto min-w-[6rem]", data: { auto_submit_form_target: "trigger" } } %>
      </div>
      <div class="flex flex-col gap-1">
        <span class="text-sm font-medium text-slate-700">&nbsp;</span>
        <div class="flex gap-2">
          <%= f.submit t("common.filter"), class: "rounded-md bg-slate-800 px-3 py-2 text-sm font-medium text-white hover:bg-slate-700 focus:outline-none focus:ring-2 focus:ring-slate-500" %>
          <%= link_to t("common.clear_filters"), stocks_path(view: "transactions", portfolio_id: @stock_portfolio.id), class: "rounded-md border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50 focus:outline-none focus:ring-2 focus:ring-slate-500" %>
        </div>
      </div>
    <% end %>

    <% if @trades.present? %>
      <% price_col = @stock_portfolio.argentina? ? t("stocks.index.columns.price_ars") : t("stocks.index.columns.price_usd") %>
      <% total_col = @stock_portfolio.argentina? ? t("stocks.index.columns.total_value_ars") : t("stocks.index.columns.total_value_usd") %>
      <%= render DataTableComponent.new(columns: [
        { label: t("stocks.index.columns.date"),   classes: "text-left" },
        { label: t("stocks.index.columns.ticker"),  classes: "text-left" },
        { label: t("stocks.index.form.side"),       classes: "text-left" },
        { label: @stock_portfolio.argentina? ? t("stocks.index.columns.quantity") : t("stocks.index.columns.shares"), classes: "text-right" },
        { label: price_col, classes: "text-right" },
        { label: total_col, classes: "text-right" }
      ]) do |table| %>
        <% @trades.each do |trade| %>
          <% table.with_row(classes: trade.side == "buy" ? "bg-emerald-50/50" : "bg-red-50/50") do %>
            <td class="whitespace-nowrap px-6 py-4 text-sm text-slate-700"><%= trade.executed_at.strftime("%b %d, %Y") %></td>
            <td class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-slate-900"><%= trade.ticker %></td>
            <td class="whitespace-nowrap px-6 py-4 text-sm font-medium capitalize <%= trade.side == 'buy' ? 'text-emerald-700' : 'text-red-700' %>"><%= trade.side %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700"><%= number_with_precision(trade.shares, precision: 6, strip_insignificant_zeros: true) %></td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700">
              <%= @stock_portfolio.argentina? ? format_ars(trade.price_usd) : format_money(trade.price_usd) %>
            </td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700">
              <%= @stock_portfolio.argentina? ? format_ars(trade.total_value_usd) : format_money(trade.total_value_usd) %>
            </td>
          <% end %>
        <% end %>
      <% end %>
      <% if @pagy.last > 1 %>
        <div class="mt-4 flex justify-center">
          <%== @pagy.series_nav %>
        </div>
      <% end %>
    <% else %>
      <%= render EmptyStateComponent.new(message: t("stocks.index.empty_trades")) %>
    <% end %>
  <% end %>

  <% if @view == "performance" %>
    <div class="mb-6 flex flex-wrap items-center gap-3">
      <%= render ModalComponent.new(title: t("stocks.index.performance.record_entry"), trigger_label: t("stocks.index.performance.record_entry")) do %>
        <%= form_with url: stocks_snapshots_path, method: :post, local: true, data: { turbo: false, controller: "performance-entry" } do |f| %>
          <%= hidden_field_tag :portfolio_id, @stock_portfolio.id %>
          <div class="space-y-4">
            <div>
              <label class="block text-sm font-medium text-slate-700"><%= t("stocks.index.performance.type") %></label>
              <div class="mt-1 flex gap-4">
                <label class="flex items-center gap-2 text-sm">
                  <%= radio_button_tag :entry_type, "snapshot", true, data: { action: "change->performance-entry#typeChanged", "performance-entry-target": "type" }, class: "text-slate-800" %>
                  <%= t("stocks.index.performance.snapshot_only") %>
                </label>
                <label class="flex items-center gap-2 text-sm">
                  <%= radio_button_tag :entry_type, "deposit", false, data: { action: "change->performance-entry#typeChanged", "performance-entry-target": "type" }, class: "text-slate-800" %>
                  <%= t("stocks.index.performance.deposit") %>
                </label>
                <label class="flex items-center gap-2 text-sm">
                  <%= radio_button_tag :entry_type, "withdrawal", false, data: { action: "change->performance-entry#typeChanged", "performance-entry-target": "type" }, class: "text-slate-800" %>
                  <%= t("stocks.index.performance.withdrawal") %>
                </label>
              </div>
            </div>
            <div data-performance-entry-target="amountField" class="hidden">
              <label class="block text-sm font-medium text-slate-700">
                <%= t("stocks.index.performance.amount", currency: @stock_portfolio.argentina? ? "ARS" : "USD") %>
              </label>
              <input type="number" name="cash_flow_amount" step="any" min="0" placeholder="0"
                     class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:border-slate-500 focus:outline-none focus:ring-1 focus:ring-slate-500">
              <p class="mt-1 text-xs text-slate-500"><%= t("stocks.index.performance.amount_hint") %></p>
            </div>
            <p class="text-xs text-slate-500"><%= t("stocks.index.performance.auto_fetch") %></p>
          </div>
          <div class="mt-6 flex gap-2">
            <button type="submit" class="rounded-md bg-slate-800 px-4 py-2 text-sm font-medium text-white hover:bg-slate-700 focus:outline-none focus:ring-2 focus:ring-slate-500"><%= t("common.save") %></button>
            <button type="button" data-action="dialog#close" class="rounded-md border border-slate-300 bg-white px-4 py-2 text-sm text-slate-700 hover:bg-slate-50"><%= t("common.cancel") %></button>
          </div>
        <% end %>
      <% end %>
    </div>

    <% charts_data = {
      twr: @twr_series.map { |p| { date: p.date, twr_pct: p.twr_pct } },
      allocation: @allocation_series
    } %>
    <%= content_tag :div,
          data: { controller: "stocks-charts", "stocks-charts-data-value": charts_data.to_json } do %>
      <div class="mb-6 rounded-lg border border-slate-200 bg-white p-4 shadow-sm">
        <p class="mb-3 text-sm font-medium text-slate-700"><%= t("stocks.index.performance.allocation_over_time") %></p>
        <div class="h-56">
          <canvas data-stocks-charts-target="allocationCanvas"></canvas>
          <p class="hidden text-sm text-slate-500" data-stocks-charts-target="allocationEmpty"><%= t("stocks.index.performance.no_snapshots_allocation") %></p>
        </div>
      </div>
      <div class="mb-6 rounded-lg border border-slate-200 bg-white p-4 shadow-sm">
        <p class="mb-3 text-sm font-medium text-slate-700"><%= t("stocks.index.performance.twr") %></p>
        <div class="h-56">
          <canvas data-stocks-charts-target="twrCanvas"></canvas>
          <p class="hidden text-sm text-slate-500" data-stocks-charts-target="twrEmpty"><%= t("stocks.index.performance.no_snapshots_twr") %></p>
        </div>
      </div>
    <% end %>

    <% if @snapshots.any? %>
      <%= render DataTableComponent.new(columns: [
        { label: t("stocks.index.columns.date"),            classes: "text-left" },
        { label: t("stocks.index.performance.type"),        classes: "text-left" },
        { label: t("stocks.index.performance.cash_flow"),   classes: "text-right" },
        { label: t("stocks.index.performance.portfolio_value"), classes: "text-right" },
        { label: t("stocks.index.performance.source"),      classes: "text-left" },
        { label: t("stocks.index.performance.positions"),   classes: "text-left" },
        { label: "", classes: "text-right" }
      ]) do |table| %>
        <% @snapshots.each do |snap| %>
          <% breakdown = snap.positions_breakdown %>
          <% table.with_row do %>
            <td class="whitespace-nowrap px-6 py-4 text-sm text-slate-700"><%= snap.recorded_at.strftime("%b %d, %Y %H:%M") %></td>
            <td class="whitespace-nowrap px-6 py-4 text-sm text-slate-700">
              <% if snap.deposit? %>
                <span class="inline-flex items-center rounded-full bg-emerald-50 px-2 py-0.5 text-xs font-medium text-emerald-700"><%= t("stocks.index.performance.deposit") %></span>
              <% elsif snap.withdrawal? %>
                <span class="inline-flex items-center rounded-full bg-red-50 px-2 py-0.5 text-xs font-medium text-red-700"><%= t("stocks.index.performance.withdrawal") %></span>
              <% else %>
                <span class="inline-flex items-center rounded-full bg-slate-100 px-2 py-0.5 text-xs font-medium text-slate-600"><%= t("stocks.index.performance.snapshot") %></span>
              <% end %>
            </td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm <%= snap.snapshot_only? ? 'text-slate-400' : (snap.deposit? ? 'text-emerald-600' : 'text-red-600') %>">
              <% if snap.snapshot_only? %>
                —
              <% elsif @stock_portfolio.argentina? %>
                <%= snap.deposit? ? "+" : "−" %><%= format_ars(snap.cash_flow.abs) %>
              <% else %>
                <%= snap.deposit? ? "+" : "−" %><%= format_money(snap.cash_flow.abs) %>
              <% end %>
            </td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm text-slate-700">
              <%= @stock_portfolio.argentina? ? format_ars(snap.total_value) : format_money(snap.total_value) %>
            </td>
            <td class="whitespace-nowrap px-6 py-4 text-sm capitalize text-slate-500"><%= snap.source %></td>
            <td class="px-6 py-4 text-sm text-slate-700">
              <% if breakdown.any? %>
                <details class="group">
                  <summary class="cursor-pointer list-none text-xs text-slate-500 hover:text-slate-800">
                    <span class="group-open:hidden">&#9654; <%= t("stocks.index.performance.n_positions", count: breakdown.size) %></span>
                    <span class="hidden group-open:inline">&#9660; <%= t("common.hide") %></span>
                  </summary>
                  <table class="mt-2 w-full min-w-[18rem] text-xs">
                    <thead>
                      <tr class="text-slate-500">
                        <th class="pb-1 text-left font-medium"><%= t("stocks.index.columns.ticker") %></th>
                        <th class="pb-1 text-right font-medium"><%= t("stocks.index.performance.value") %></th>
                        <th class="pb-1 text-right font-medium"><%= t("stocks.index.columns.pct_total") %></th>
                      </tr>
                    </thead>
                    <tbody>
                      <% breakdown.sort_by { |p| -p["pct_of_total"].to_f }.each do |pos| %>
                        <tr class="border-t border-slate-100">
                          <td class="py-1 pr-4 font-semibold text-slate-800"><%= pos["ticker"] %></td>
                          <td class="py-1 pr-4 text-right text-slate-700">
                            <%= @stock_portfolio.argentina? ? format_ars(pos["value"]) : format_money(pos["value"]) %>
                          </td>
                          <td class="py-1 text-right text-slate-500"><%= number_to_percentage(pos["pct_of_total"], precision: 1) %></td>
                        </tr>
                      <% end %>
                    </tbody>
                  </table>
                </details>
              <% else %>
                <span class="text-xs text-slate-400">—</span>
              <% end %>
            </td>
            <td class="whitespace-nowrap px-6 py-4 text-right text-sm">
              <%= button_to t("common.delete"), stocks_snapshot_path(id: snap.id, portfolio_id: @stock_portfolio.id),
                    method: :delete,
                    data: { turbo_confirm: t("stocks.index.performance.delete_confirm") },
                    class: "text-slate-400 hover:text-red-600 text-xs" %>
            </td>
          <% end %>
        <% end %>
      <% end %>
    <% else %>
      <%= render EmptyStateComponent.new(message: t("stocks.index.performance.empty")) %>
    <% end %>
  <% end %>

  <% if @view == "valuations" %>
    <% if @positions.empty? %>
      <%= render EmptyStateComponent.new(message: "No open positions to show valuations for.") %>
    <% else %>
      <%= render "stocks/fundamentals_table",
            tickers:       @positions.map(&:ticker),
            fundamentals:  @fundamentals,
            sync_url:      stocks_sync_fundamentals_path(portfolio_id: @stock_portfolio.id),
            analyses:      @analyses %>
    <% end %>
  <% end %>

  <% if @view == "watchlist" %>
    <%= form_with url: stocks_watchlist_path, method: :post, class: "mb-4 flex gap-2" do |f| %>
      <%= f.text_field :ticker,
            placeholder: "e.g. NVDA",
            class: "rounded-md border border-slate-300 px-3 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-slate-500 uppercase",
            autocomplete: "off",
            required: true %>
      <%= f.submit t("common.add"),
            class: "rounded-md bg-slate-800 px-3 py-1.5 text-sm font-medium text-white hover:bg-slate-700 cursor-pointer" %>
    <% end %>
    <% if @watchlist_tickers.empty? %>
      <%= render EmptyStateComponent.new(message: t("stocks.index.watchlist.empty")) %>
    <% else %>
      <%= render "stocks/fundamentals_table",
            tickers:         @watchlist_tickers.map(&:ticker),
            fundamentals:    @fundamentals,
            sync_url:        stocks_watchlist_sync_path,
            watchlist_items: @watchlist_tickers.index_by(&:ticker),
            analyses:        @analyses %>
    <% end %>
  <% end %>
</div>
