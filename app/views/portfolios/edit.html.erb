<div class="mx-auto max-w-4xl">
  <%= render BreadcrumbComponent.new(items: [
    { label: "Trades", url: trades_path },
    { label: "Portfolios", url: portfolios_path },
    { label: "Edit", url: nil }
  ]) %>
  <h1 class="mb-2 text-2xl font-semibold text-slate-900">Edit portfolio</h1>
  <p class="mb-6 text-sm text-slate-600"><%= @portfolio.name %></p>
  <%= render "form" %>
</div>
