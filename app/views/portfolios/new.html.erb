<div class="mx-auto max-w-4xl">
  <%= render BreadcrumbComponent.new(items: [
    { label: t("nav.trades"), url: trades_path },
    { label: t("portfolios.index.title"), url: portfolios_path },
    { label: t("portfolios.new.breadcrumb"), url: nil }
  ]) %>
  <h1 class="mb-2 text-2xl font-semibold text-slate-900"><%= t("portfolios.new.title") %></h1>
  <p class="mb-6 text-sm text-slate-600"><%= t("portfolios.new.subtitle") %></p>
  <%= render "form" %>
</div>
