<div class="mx-auto max-w-2xl">
  <%= render BreadcrumbComponent.new(items: [
    { label: "Trades", url: trades_path },
    { label: "Portfolios", url: portfolios_path },
    { label: "New", url: nil }
  ]) %>
  <h1 class="mb-2 text-2xl font-semibold text-slate-900">New portfolio</h1>
  <p class="mb-6 text-sm text-slate-600">Define a date range and optional starting balance. Trades in this range will show with balance = initial balance + P&L.</p>
  <%= render "form" %>
</div>
