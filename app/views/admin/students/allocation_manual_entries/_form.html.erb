<% field_classes = "mt-1 block w-full rounded-md border border-slate-300 px-3 py-2 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-1 focus:ring-indigo-500" %>
<% label_classes = "block text-sm font-medium text-slate-700" %>

<%= form_with model: @entry, url: url, method: method, class: "space-y-4" do |f| %>
  <% if @entry.errors.any? %>
    <div class="rounded-md border border-red-200 bg-red-50 p-4">
      <ul class="list-inside list-disc space-y-1 text-sm text-red-700">
        <% @entry.errors.full_messages.each do |msg| %><li><%= msg %></li><% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :label, "Label", class: label_classes %>
    <%= f.text_field :label, required: true, class: field_classes %>
  </div>
  <div>
    <%= f.label :amount_usd, "Amount (USD)", class: label_classes %>
    <%= f.number_field :amount_usd, required: true, step: "any", min: 0, class: field_classes %>
  </div>
  <% if @buckets.any? %>
    <div>
      <%= f.label :allocation_bucket_id, "Bucket", class: label_classes %>
      <%= f.select :allocation_bucket_id,
            options_from_collection_for_select(@buckets, :id, :name, @entry.allocation_bucket_id),
            { prompt: "Select" }, required: true, class: field_classes %>
    </div>
  <% end %>
  <div class="mt-6 flex items-center gap-3">
    <%= f.submit submit_label, class: "rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700 cursor-pointer" %>
    <%= link_to "Cancel", admin_student_path(@student), class: "text-sm text-slate-500 hover:text-slate-800" %>
  </div>
<% end %>
