<div class="mx-auto max-w-4xl">
  <%= render BreadcrumbComponent.new(items: [
    { label: t("companies.index.title"), url: companies_path },
    { label: t("companies.new.breadcrumb"), url: nil }
  ]) %>
  <div class="mb-6">
    <h1 class="text-2xl font-semibold text-slate-900"><%= t("companies.new.title") %></h1>
  </div>
  <%= render "form" %>
</div>
