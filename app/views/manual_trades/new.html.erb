<div class="mx-auto max-w-4xl">
  <%= render BreadcrumbComponent.new(items: [
    { label: t("nav.trades"), url: trades_path },
    { label: @account.provider_type.to_s.capitalize, url: nil },
    { label: t("manual_trades.new.breadcrumb"), url: nil }
  ]) %>
  <h1 class="mb-6 text-2xl font-semibold text-slate-900"><%= t("manual_trades.new.title") %></h1>
  <%= render "form" %>
</div>
