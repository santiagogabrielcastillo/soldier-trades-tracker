<div class="mx-auto max-w-4xl">
  <%= render BreadcrumbComponent.new(items: [
    { label: "Trades", url: trades_path },
    { label: @account.provider_type.to_s.capitalize, url: nil },
    { label: "New Trade", url: nil }
  ]) %>
  <h1 class="mb-6 text-2xl font-semibold text-slate-900">Add Manual Trade</h1>
  <%= render "form" %>
</div>
